# AGENTS

- Prefer package scripts over direct tool commands when an equivalent script exists in `package.json`.
- For local web app verification, start the app with `npm run dev` and pass flags through the script as needed.
